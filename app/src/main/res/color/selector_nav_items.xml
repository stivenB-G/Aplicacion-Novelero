<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color cuando el icono está seleccionado -->
    <item android:color="@color/novelero_primary" android:state_checked="true" />
    <!-- Color cuando el icono no está seleccionado ) -->
    <item android:color="@color/novelero_text_secondary" />
</selector>